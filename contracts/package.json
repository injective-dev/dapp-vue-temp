{
  "name": "@dapp-template/contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Injective dApp template",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network injectiveTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network injectiveMainnet",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
